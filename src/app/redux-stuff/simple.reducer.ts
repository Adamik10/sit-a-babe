// import { Action } from '@ngrx/store';

export function SimpleReducer( state: string = 'Hello world'/*, action: Action*/ ) {

    //the point is to take the current state and then based on the action changes copy it over to the new state  
    /*

    switch (action.type){
        case 'SPANISH':
            return state = 'Hole Mundo'

        case 'FRENCH':
            return state = 'Bonjour le monde'

        default:
            return state;
    }
    */
}